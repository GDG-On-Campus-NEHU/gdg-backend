# Django
DJANGO_SECRET_KEY=change-this-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
SITE_NAME=Google Developer's Group, NEHU

# Database (preferred)
# Examples:
#   PostgreSQL: postgres://user:password@localhost:5432/gdg_backend
#   MySQL: mysql://user:password@localhost:3306/gdg_backend
#   SQLite: sqlite:///db.sqlite3
DATABASE_URL=sqlite:///db.sqlite3

# Database (fallback, only used when DATABASE_URL is empty)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# CORS / CSRF
CORS_ALLOW_ALL_ORIGINS=True
CSRF_TRUSTED_ORIGINS=https://barbless-martine-unartificial.ngrok-free.dev

# Caching
# Example: redis://127.0.0.1:6379/1
REDIS_URL=
